version: "3.9"

services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      # ВАЖНО: volume монтирует папку data/ наружу контейнера,
      # чтобы база данных НЕ терялась при пересборке/перезапуске
      - bot_data:/app/data

volumes:
  bot_data:
